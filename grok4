#include <iostream>
#include <fstream>
#include <unordered_set>
#include <vector>
#include <algorithm>

using namespace std;

int main() {

    ifstream input("input.txt");
    ofstream output("output.txt");

    unordered_set<int> numbers;
    int num;

    while (input >> num) {
        if (num > 0) {
 
            numbers.insert(num);
        }
        else if (num < 0) {

            int positive_num = -num;
            numbers.erase(positive_num);
        }
        else {
            break;
        }
    }

    vector<int> result(numbers.begin(), numbers.end());
   
    sort(result.begin(), result.end());

    for (int i = 0; i < result.size(); i++) {
        output << result[i];
        if (i < result.size() - 1) {
            output << " ";
        }
    }

    input.close();
    output.close();

    return 0;
}
